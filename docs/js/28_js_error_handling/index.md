# 错误处理

这里整理 JavaScript 中与错误处理相关的核心内容，包括：

- `try / catch / finally`
- Error 对象
- 同步错误
- 异步错误
- 错误传播

后续可继续补充：

- Promise 错误链
- `async/await` 错误处理
- `throw` 的语义
- 全局错误捕获
- 浏览器中的 `error` / `unhandledrejection`
- Node.js 中的异常处理
