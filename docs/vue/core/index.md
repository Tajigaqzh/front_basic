# Vue Core

这个目录用于整理 Vue 使用，包括模板语法、组合式 API、组件通信和常见开发模式。

## 可补充内容

- 模板语法
- `ref` 与 `reactive`
- 计算属性与侦听器
- 组件通信
- 生命周期

## 导航

- 返回 [Vue 模块](../index.md)


# v-if和v-show的区别

对比               v-if                                v-show                                                                                                     
━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━
是否渲染 DOM       条件为 false 时不渲染/会销毁 DOM    始终渲染 DOM
─────────────────  ──────────────────────────────────  ────────────────────────────
控制方式           创建或移除元素                      切换 CSS：display: none
─────────────────  ──────────────────────────────────  ────────────────────────────
初始渲染成本       较低，条件为 false 时不创建         较高，不管是否显示都会创建
─────────────────  ──────────────────────────────────  ────────────────────────────
切换成本           较高，需要创建/销毁 DOM             较低，只改样式
─────────────────  ──────────────────────────────────  ────────────────────────────
是否能在template
上使用                  支持                             不支持
─────────────────  ──────────────────────────────────  ────────────────────────────