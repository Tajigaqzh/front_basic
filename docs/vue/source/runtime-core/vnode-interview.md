# vnode.ts 常见面试题

## 1. 什么是 VNode？

VNode 是对真实 UI 结构的运行时描述对象，是 Vue diff 的最小输入单元。

## 2. 为什么创建 VNode 时要提前归一化 children？

这样后续 patch 阶段可以直接基于统一结构处理，避免在高频更新路径里重复做类型清洗。

## 3. `key` 的作用是什么？

`key` 用来稳定同层节点身份，帮助 diff 正确复用、移动和删除节点。
