# renderer.ts 常见面试题

## 1. `patch()` 为什么是渲染器核心？

因为挂载、更新、卸载、节点替换，最终都统一收敛到 `patch()`，它是整棵 VNode 树递归 diff 的入口。

## 2. `shapeFlag` 的作用是什么？

它把节点大类编码成位标记，让运行时能快速判断当前节点是元素、组件、Teleport 还是 children 类型，减少大量字符串判断。

## 3. 为什么要把宿主能力抽出来？

这样 `runtime-core` 就不绑定浏览器，可以复用到 DOM、SSR、自定义渲染器等不同平台。
