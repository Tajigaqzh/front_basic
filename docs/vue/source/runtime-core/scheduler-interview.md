# scheduler.ts 常见面试题

## 1. 为什么 Vue 不在每次状态变化后立刻更新 DOM？

因为同步立刻更新会导致重复 render。调度器会把同一轮里的多个更新合并，统一刷新。

## 2. `nextTick()` 本质是什么？

本质上是等待当前这轮调度刷新完成后再执行回调。

## 3. 为什么父组件通常先于子组件更新？

因为调度队列会按 job id 排序，而父组件实例总是先创建，所以更新优先级更高。
